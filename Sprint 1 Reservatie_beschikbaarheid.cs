

// US-1.3 Reserveren Geeft aan wanneer de tafel (niet) beschikbaar is
// all_reservations => list van Berkan
public void reserveren(int reservatie) 
{
    foreach(reservatie in all_reservations)
    {
        if(reservatie in all_reservations)
        {
            Console.WriteLine("De tafel is niet beschikbaar");
        }
        else
        {
            Console.WriteLine("Beschikbaar");
        }
    }
}
