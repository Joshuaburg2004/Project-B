If you want to run this code in VS: Set the contents of the JSON map (excluding ControllerJson.cs) into the map:
solution map > bin > Debug > netX.0
This is to allow the Json files to be read and written to.
